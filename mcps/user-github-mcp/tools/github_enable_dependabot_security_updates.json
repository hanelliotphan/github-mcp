{
  "name": "github_enable_dependabot_security_updates",
  "description": "Enable Dependabot security updates for a repository (PUT /repos/{owner}/{repo}/automated-security-fixes). Requires admin access. If GitHub returns 422 about vulnerability alerts, call github_enable_vulnerability_alerts first. Returns 204 with no body on success.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}