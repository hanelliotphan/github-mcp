{
  "name": "github_get_repo_codespace_defaults",
  "description": "Get default properties for a new codespace in a repository (GET /repos/{owner}/{repo}/codespaces/new). Optional ref and client_ip query params. See GitHub REST Codespaces.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      },
      "ref": {
        "type": "string"
      },
      "client_ip": {
        "type": "string"
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}