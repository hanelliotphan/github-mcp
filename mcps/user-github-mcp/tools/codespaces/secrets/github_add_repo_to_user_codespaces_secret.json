{
  "name": "github_add_repo_to_user_codespaces_secret",
  "description": "Add repository to user codespaces secret. See GitHub REST secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "secret_name": {
        "type": "string",
        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
      },
      "repository_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": [
      "secret_name",
      "repository_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}