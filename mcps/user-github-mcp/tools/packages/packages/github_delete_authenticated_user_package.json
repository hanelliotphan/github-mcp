{
  "name": "github_delete_authenticated_user_package",
  "description": "Delete a package for the authenticated user (DELETE /user/packages/{package_type}/{package_name}). Success is HTTP **204**.",
  "arguments": {
    "type": "object",
    "properties": {
      "package_type": {
        "type": "string",
        "enum": [
          "npm",
          "maven",
          "rubygems",
          "docker",
          "nuget",
          "container"
        ]
      },
      "package_name": {
        "type": "string",
        "minLength": 1
      }
    },
    "$schema": "http://json-schema.org/draft-07/schema#",
    "required": [
      "package_type",
      "package_name"
    ]
  }
}
