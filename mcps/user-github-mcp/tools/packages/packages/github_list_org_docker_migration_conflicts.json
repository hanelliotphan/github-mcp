{
  "name": "github_list_org_docker_migration_conflicts",
  "description": "List packages that conflict during Docker migration for an organization (GET /orgs/{org}/docker/conflicts).",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39
      }
    },
    "$schema": "http://json-schema.org/draft-07/schema#",
    "required": [
      "org"
    ]
  }
}
