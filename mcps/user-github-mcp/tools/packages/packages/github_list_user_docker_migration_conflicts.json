{
  "name": "github_list_user_docker_migration_conflicts",
  "description": "List packages that conflict during Docker migration for a user (GET /users/{username}/docker/conflicts).",
  "arguments": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39
      }
    },
    "$schema": "http://json-schema.org/draft-07/schema#",
    "required": [
      "username"
    ]
  }
}
