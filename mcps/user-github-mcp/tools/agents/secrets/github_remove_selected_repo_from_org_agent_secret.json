{
  "name": "github_remove_selected_repo_from_org_agent_secret",
  "description": "Remove a single repository from a selected-visibility organization GitHub agent secret (DELETE /orgs/{org}/agents/secrets/{secret_name}/repositories/{repository_id}). Visibility must be selected (else 409). Classic tokens need admin:org scope (plus repo for private repos). Success is HTTP 204. See GitHub REST Agents secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "secret_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "repository_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "secret_name", "repository_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
