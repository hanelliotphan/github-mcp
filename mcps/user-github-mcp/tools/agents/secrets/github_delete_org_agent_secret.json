{
  "name": "github_delete_org_agent_secret",
  "description": "Delete an organization GitHub agent secret by name (DELETE /orgs/{org}/agents/secrets/{secret_name}). Classic tokens need admin:org scope (plus repo for private repos). Success is HTTP 204. See GitHub REST Agents secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "secret_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" }
    },
    "required": ["org", "secret_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
