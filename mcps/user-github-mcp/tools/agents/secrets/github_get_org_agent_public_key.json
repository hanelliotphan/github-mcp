{
  "name": "github_get_org_agent_public_key",
  "description": "Get an organization's public key for encrypting GitHub agent secrets (GET /orgs/{org}/agents/secrets/public-key). Returns key_id and key (base64) needed before creating/updating a secret. Classic tokens need admin:org scope (plus repo for private repos). See GitHub REST Agents secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
