{
  "name": "github_get_repo_agent_public_key",
  "description": "Get a repository's public key for encrypting GitHub agent secrets (GET /repos/{owner}/{repo}/agents/secrets/public-key). Returns key_id and key (base64) needed before creating/updating a secret. Anyone with read access; private repos need repo scope for classic tokens. See GitHub REST Agents secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
