{
  "name": "github_list_org_agent_variables",
  "description": "List GitHub Agents variables in an organization (GET /orgs/{org}/agents/variables). Returns total_count and variables (name, value, timestamps, visibility). per_page 1-30 default 30, page, all_pages, max_pages. Classic tokens need admin:org scope (plus repo for private repositories). See GitHub REST Agents variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "per_page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 30
      },
      "page": {
        "type": "integer",
        "minimum": 1
      },
      "all_pages": {
        "type": "boolean"
      },
      "max_pages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 500
      }
    },
    "required": [
      "org"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
