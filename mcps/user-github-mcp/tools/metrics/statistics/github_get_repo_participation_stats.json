{
  "name": "github_get_repo_participation_stats",
  "description": "Get weekly commit counts for owner vs everyone (GET /repos/{owner}/{repo}/stats/participation).",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39 },
      "name": { "type": "string", "minLength": 1, "maxLength": 100 }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
