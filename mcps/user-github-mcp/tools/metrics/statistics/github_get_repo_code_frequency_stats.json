{
  "name": "github_get_repo_code_frequency_stats",
  "description": "Get weekly additions/deletions for a repository (GET /repos/{owner}/{repo}/stats/code_frequency).",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39 },
      "name": { "type": "string", "minLength": 1, "maxLength": 100 }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
