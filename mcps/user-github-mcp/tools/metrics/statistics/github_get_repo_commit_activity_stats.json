{
  "name": "github_get_repo_commit_activity_stats",
  "description": "Get the last year of commit activity by week (GET /repos/{owner}/{repo}/stats/commit_activity).",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39 },
      "name": { "type": "string", "minLength": 1, "maxLength": 100 }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
