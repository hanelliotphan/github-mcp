{
  "name": "github_get_repo_punch_card_stats",
  "description": "Get hourly commit counts for each day of the week (GET /repos/{owner}/{repo}/stats/punch_card).",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39 },
      "name": { "type": "string", "minLength": 1, "maxLength": 100 }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
