{
  "name": "github_redeliver_app_webhook_delivery",
  "description": "Redeliver a GitHub App webhook delivery (POST /app/hook/deliveries/{delivery_id}/attempts). Queues a new attempt; success is typically HTTP 202. delivery_id from list or get delivery. Requires JWT (GitHub App auth). See GitHub REST Apps webhooks.",
  "arguments": {
    "type": "object",
    "properties": {
      "delivery_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["delivery_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
