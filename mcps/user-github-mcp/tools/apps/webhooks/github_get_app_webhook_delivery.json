{
  "name": "github_get_app_webhook_delivery",
  "description": "Get one GitHub App webhook delivery by ID (GET /app/hook/deliveries/{delivery_id}). Full delivery including request/response bodies when present. delivery_id from github_list_app_webhook_deliveries id. Requires JWT (GitHub App auth). HTTP 200.",
  "arguments": {
    "type": "object",
    "properties": {
      "delivery_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["delivery_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
