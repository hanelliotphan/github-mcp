{
  "name": "github_get_user_installation_for_authenticated_app",
  "description": "GET /users/{username}/installation — get the authenticated GitHub App installation for a user. Requires JWT. username is GitHub login (1-39 chars).",
  "arguments": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": [
      "username"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
