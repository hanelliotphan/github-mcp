{
  "name": "github_get_app",
  "description": "GET /apps/{app_slug} — get a GitHub App by its URL-friendly slug (from app settings). See get-an-app.",
  "arguments": {
    "type": "object",
    "properties": {
      "app_slug": {
        "type": "string",
        "minLength": 1,
        "description": "URL-friendly name of the GitHub App."
      }
    },
    "required": [
      "app_slug"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
