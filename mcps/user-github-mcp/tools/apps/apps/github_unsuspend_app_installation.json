{
  "name": "github_unsuspend_app_installation",
  "description": "DELETE /app/installations/{installation_id}/suspended — remove installation suspension (204). Requires JWT.",
  "arguments": {
    "type": "object",
    "properties": {
      "installation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the installation."
      }
    },
    "required": [
      "installation_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
