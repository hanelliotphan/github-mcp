{
  "name": "github_get_installation_for_authenticated_app",
  "description": "GET /app/installations/{installation_id} — get an installation for the authenticated GitHub App. Requires JWT. installation_id required.",
  "arguments": {
    "type": "object",
    "properties": {
      "installation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the installation."
      }
    },
    "required": [
      "installation_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
