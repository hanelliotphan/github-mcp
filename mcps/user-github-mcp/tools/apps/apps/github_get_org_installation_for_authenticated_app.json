{
  "name": "github_get_org_installation_for_authenticated_app",
  "description": "GET /orgs/{org}/installation — get the authenticated GitHub App installation for an organization. Requires JWT. org is organization login (1-39 chars).",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": [
      "org"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
