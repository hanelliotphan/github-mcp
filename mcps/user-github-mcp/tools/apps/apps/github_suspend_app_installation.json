{
  "name": "github_suspend_app_installation",
  "description": "PUT /app/installations/{installation_id}/suspended — suspend installation (204). Requires JWT. Blocks API and webhook access for the account.",
  "arguments": {
    "type": "object",
    "properties": {
      "installation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the installation."
      }
    },
    "required": [
      "installation_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
