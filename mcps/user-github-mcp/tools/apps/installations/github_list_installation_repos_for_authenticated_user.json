{
  "name": "github_list_installation_repos_for_authenticated_user",
  "description": "GET /user/installations/{installation_id}/repositories — list repositories the authenticated user can access for an installation. Returns total_count and repositories. Requires user access token. Pagination: page, per_page 1-100 default 100; all_pages and max_pages.",
  "arguments": {
    "type": "object",
    "properties": {
      "installation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the installation."
      },
      "per_page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100
      },
      "page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 9007199254740991
      },
      "all_pages": {
        "type": "boolean"
      },
      "max_pages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 500
      }
    },
    "required": [
      "installation_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
