{
  "name": "github_add_repo_to_app_installation",
  "description": "PUT /user/installations/{installation_id}/repositories/{repository_id} — add a repository to an app installation (204). Requires classic PAT with repo scope and admin access to the repository.",
  "arguments": {
    "type": "object",
    "properties": {
      "installation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the installation."
      },
      "repository_id": {
        "type": "integer",
        "minimum": 1,
        "description": "The unique identifier of the repository."
      }
    },
    "required": [
      "installation_id",
      "repository_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
