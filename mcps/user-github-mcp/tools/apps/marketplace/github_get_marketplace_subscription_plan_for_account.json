{
  "name": "github_get_marketplace_subscription_plan_for_account",
  "description": "GET /marketplace_listing/accounts/{account_id} — get Marketplace subscription plan for an account. Requires GitHub App JWT or OAuth app basic auth (client id/secret).",
  "arguments": {
    "type": "object",
    "properties": {
      "account_id": {
        "type": "integer",
        "description": "Account id (user or organization)."
      }
    },
    "required": [
      "account_id"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
