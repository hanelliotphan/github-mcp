{
  "name": "github_get_agent_task_for_repo",
  "description": "Get a Copilot cloud agent task by ID scoped to a repository (GET /agents/repos/{owner}/{repo}/tasks/{task_id}). Public preview. Requires fine-grained PAT or GitHub App user token with Agent tasks repository read permission. Returns task with sessions. HTTP 200. See GitHub REST agent-tasks.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "task_id": { "type": "string", "minLength": 1 }
    },
    "required": ["owner", "name", "task_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
