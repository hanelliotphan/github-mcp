{
  "name": "github_list_repo_autolinks",
  "description": "List all autolinks configured for a repository (GET /repos/{owner}/{repo}/autolinks). Autolink metadata is only available to repository administrators. Fine-grained tokens need appropriate admin/repo permissions per GitHub. See GitHub REST docs for repository autolinks.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}