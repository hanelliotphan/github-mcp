{
  "name": "github_replace_repo_topics",
  "description": "Replace all repository topics (PUT /repos/{owner}/{repo}/topics). Replaces the entire topic set with `names`; pass an empty array to clear all topics. At most 20 topic names (GitHub limit). Requires write access; classic tokens need the repo scope.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      },
      "names": {
        "maxItems": 20,
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 50
        },
        "description": "Topic names to set (replaces existing). Use [] to clear. GitHub persists lowercase names."
      }
    },
    "required": [
      "owner",
      "name",
      "names"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}