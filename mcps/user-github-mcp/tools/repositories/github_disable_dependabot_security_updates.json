{
  "name": "github_disable_dependabot_security_updates",
  "description": "Disable Dependabot security updates for a repository (DELETE /repos/{owner}/{repo}/automated-security-fixes). Requires admin access. Does not disable dependency vulnerability alerts; use the vulnerability-alerts API separately if needed. Returns 204 with no body on success.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}