{
  "name": "github_test_push_repo_webhook",
  "description": "Test the push repository webhook (POST .../hooks/{hook_id}/tests). Triggers a test push delivery if subscribed to push; else 204 with no POST. hook_id from github_list_repo_webhooks. Classic tokens need write:repo_hook or repo. See GitHub REST repository webhooks.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      },
      "hook_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["owner", "name", "hook_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
