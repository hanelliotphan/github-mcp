{
  "name": "github_list_repo_code_scanning_dismissal_requests",
  "description": "List dismissal requests for code scanning alerts for a repository (GET /repos/{owner}/{repo}/dismissal-requests/code-scanning).",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "reviewer": {
        "type": "string",
        "minLength": 1
      },
      "requester": {
        "type": "string",
        "minLength": 1
      },
      "time_period": {
        "type": "string",
        "enum": [
          "hour",
          "day",
          "week",
          "month"
        ]
      },
      "request_status": {
        "type": "string",
        "enum": [
          "open",
          "approved",
          "expired",
          "denied",
          "all"
        ]
      },
      "per_page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100
      },
      "page": {
        "type": "integer",
        "minimum": 1
      },
      "all_pages": {
        "type": "boolean"
      },
      "max_pages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 500
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
