{
  "name": "github_get_summary_stats",
  "description": "GET /orgs/{org}/insights/api/summary-stats — org-wide API request totals (total_request_count, rate_limited_request_count). Requires min_timestamp (ISO 8601); optional max_timestamp. API Insights required.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "min_timestamp": { "type": "string", "minLength": 1 },
      "max_timestamp": { "type": "string", "minLength": 1 }
    },
    "required": ["org", "min_timestamp"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
