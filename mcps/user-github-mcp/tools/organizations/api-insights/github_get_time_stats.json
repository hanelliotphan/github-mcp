{
  "name": "github_get_time_stats",
  "description": "GET /orgs/{org}/insights/api/time-stats — org-wide API request counts over time (timestamp buckets). Requires min_timestamp (ISO 8601) and timestamp_increment (e.g. 5m, 10m, 1h); optional max_timestamp. API Insights required.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "min_timestamp": { "type": "string", "minLength": 1 },
      "max_timestamp": { "type": "string", "minLength": 1 },
      "timestamp_increment": { "type": "string", "minLength": 1 }
    },
    "required": ["org", "min_timestamp", "timestamp_increment"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
