{
  "name": "github_enable_immutable_releases_for_org_repo",
  "description": "Add one repo to org selected immutable-releases enforcement (PUT /orgs/{org}/settings/immutable-releases/repositories/{repository_id}). Requires enforced_repositories=selected. Returns 204. Classic PATs typically need admin:org.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "repository_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["org", "repository_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
