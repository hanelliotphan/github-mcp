{
  "name": "github_get_org",
  "description": "Get a GitHub organization (GET /orgs/{org}). Public fields for any org; full details may need admin:org or fine-grained org permissions. org is organization login (1-39 chars). See GitHub REST orgs.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
