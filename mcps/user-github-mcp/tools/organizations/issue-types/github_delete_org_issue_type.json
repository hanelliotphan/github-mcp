{
  "name": "github_delete_org_issue_type",
  "description": "DELETE /orgs/{org}/issue-types/{issue_type_id} — remove an org issue type. HTTP 204 on success. Org admin; classic PAT admin:org. See GitHub REST org issue types.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "issue_type_id": {
        "type": "integer",
        "minimum": 1,
        "description": "Numeric id of the issue type to delete."
      }
    },
    "required": ["org", "issue_type_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
