{
  "name": "github_cancel_org_invitation",
  "description": "DELETE /orgs/{org}/invitations/{invitation_id} — cancel a pending org invitation. HTTP 204 on success. Org owner; classic PAT admin:org. Triggers notifications. See GitHub REST org members.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "invitation_id": {
        "type": "integer",
        "minimum": 1,
        "description": "Numeric id of the pending invitation."
      }
    },
    "required": ["org", "invitation_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
