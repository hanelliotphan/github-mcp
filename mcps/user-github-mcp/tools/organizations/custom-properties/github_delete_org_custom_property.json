{
  "name": "github_delete_org_custom_property",
  "description": "DELETE /orgs/{org}/properties/schema/{custom_property_name} — remove one org custom property definition. HTTP 204 No Content on success. custom_properties_org_definitions_manager or org admin. See GitHub REST org custom properties.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "custom_property_name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 255,
        "description": "Custom property name to remove (GitHub path segment)."
      }
    },
    "required": ["org", "custom_property_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
