{
  "name": "github_get_org_custom_property",
  "description": "GET /orgs/{org}/properties/schema/{custom_property_name} — one org custom property definition. Members may read. HTTP 200. See GitHub REST org custom properties.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "custom_property_name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 255,
        "description": "Custom property name (path segment; same as property_name in API bodies)."
      }
    },
    "required": ["org", "custom_property_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
