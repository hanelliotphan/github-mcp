{
  "name": "github_list_teams_assigned_to_org_role",
  "description": "GET /orgs/{org}/organization-roles/{role_id}/teams — list teams assigned to an org role (role assignment for a team rows). Requires org admin; classic tokens need admin:org. page/per_page (default 100), all_pages + max_pages. HTTP 200.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "role_id": {
        "type": "integer",
        "minimum": 1
      },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["org", "role_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
