{
  "name": "github_get_org_role",
  "description": "GET /orgs/{org}/organization-roles/{role_id} — one organization role (id, name, description, base_role, source, permissions, organization). Requires org admin; classic tokens need admin:org. HTTP 200.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "role_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["org", "role_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
