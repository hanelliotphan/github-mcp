{
  "name": "github_remove_all_org_roles_for_team",
  "description": "DELETE /orgs/{org}/organization-roles/teams/{team_slug} — remove all assigned organization roles from a team. Requires org admin; classic tokens need admin:org. HTTP 204 No Content.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "team_slug": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": ["org", "team_slug"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
