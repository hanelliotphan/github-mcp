{
  "name": "github_remove_org_role_from_team",
  "description": "DELETE /orgs/{org}/organization-roles/teams/{team_slug}/{role_id} — remove one organization role from a team. Requires org admin; classic tokens need admin:org. HTTP 204 No Content.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "team_slug": {
        "type": "string",
        "minLength": 1
      },
      "role_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["org", "team_slug", "role_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
