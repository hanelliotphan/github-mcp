{
  "name": "github_list_organizations",
  "description": "List all GitHub organizations in creation order (GET /organizations). Pagination via since (org id) and per_page 1-100 default 100; all_pages and max_pages. See GitHub REST orgs.",
  "arguments": {
    "type": "object",
    "properties": {
      "since": {
        "type": "integer",
        "minimum": 0,
        "description": "Only return organizations with an id greater than this value."
      },
      "per_page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100
      },
      "all_pages": {
        "type": "boolean"
      },
      "max_pages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 500
      }
    },
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
