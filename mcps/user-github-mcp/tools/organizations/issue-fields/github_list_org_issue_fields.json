{
  "name": "github_list_org_issue_fields",
  "description": "GET /orgs/{org}/issue-fields — list all org issue fields (id, name, data_type, options, …). Classic tokens need read:org. HTTP 200. See GitHub REST org issue fields.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
