{
  "name": "github_get_org_membership_for_auth_user",
  "description": "GET /user/memberships/orgs/{org} — authenticated user's membership in one org (active/pending). Same object as orgs/{org}/memberships/{username} GET. 404 if not affiliated. See GitHub REST org members.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
