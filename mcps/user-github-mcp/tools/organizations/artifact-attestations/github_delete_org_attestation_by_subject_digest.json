{
  "name": "github_delete_org_attestation_by_subject_digest",
  "description": "DELETE /orgs/{org}/attestations/digest/{subject_digest} — delete attestations for one subject digest. Success 200 or 204; 404 if not found. See GitHub REST org attestations.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "subject_digest": { "type": "string", "minLength": 1 }
    },
    "required": ["org", "subject_digest"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
