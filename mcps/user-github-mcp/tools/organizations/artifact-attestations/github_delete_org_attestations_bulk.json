{
  "name": "github_delete_org_attestations_bulk",
  "description": "POST /orgs/{org}/attestations/delete-request — bulk-delete attestations for the given subject_digests[]. HTTP 200; 404 if not found. Requires org and attestations delete access per GitHub. See REST org attestations.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "subject_digests": {
        "type": "array",
        "minItems": 1,
        "maxItems": 100,
        "items": { "type": "string", "minLength": 1 }
      }
    },
    "required": ["org", "subject_digests"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
