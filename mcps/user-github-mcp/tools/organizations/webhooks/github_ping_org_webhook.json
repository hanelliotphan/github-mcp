{
  "name": "github_ping_org_webhook",
  "description": "Ping an organization webhook (POST /orgs/{org}/hooks/{hook_id}/pings). Sends a ping event to the hook URL. Success is HTTP 204. hook_id from github_list_org_webhooks. Requires org owner; classic tokens need admin:org_hook. See GitHub REST organization webhooks.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "hook_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["org", "hook_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
