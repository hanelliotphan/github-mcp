{
  "name": "github_delete_org_campaign",
  "description": "Delete a security campaign for an organization (DELETE /orgs/{org}/campaigns/{campaign_number}). Returns 204 on success.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39 },
      "campaign_number": { "type": "integer", "exclusiveMinimum": 0 }
    },
    "required": ["org", "campaign_number"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
