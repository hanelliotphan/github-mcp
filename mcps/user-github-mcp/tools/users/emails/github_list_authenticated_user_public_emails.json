{
  "name": "github_list_authenticated_user_public_emails",
  "description": "GET /user/public_emails — list public emails for authenticated user. page/per_page (default 100), all_pages + max_pages.",
  "arguments": {
    "type": "object",
    "properties": {
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
