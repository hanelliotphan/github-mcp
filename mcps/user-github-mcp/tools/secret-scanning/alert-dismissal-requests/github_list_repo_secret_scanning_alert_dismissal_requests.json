{
  "name": "github_list_repo_secret_scanning_alert_dismissal_requests",
  "description": "List secret scanning alert dismissal requests for a repository (GET /repos/{owner}/{repo}/dismissal-requests/secret-scanning). Classic tokens need security_events.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "reviewer": {
        "type": "string"
      },
      "requester": {
        "type": "string"
      },
      "time_period": {
        "type": "string",
        "enum": [
          "hour",
          "day",
          "week",
          "month"
        ]
      },
      "request_status": {
        "type": "string",
        "enum": [
          "completed",
          "cancelled",
          "approved",
          "expired",
          "denied",
          "open",
          "all"
        ]
      },
      "per_page": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100
      },
      "page": {
        "type": "integer",
        "minimum": 1
      },
      "all_pages": {
        "type": "boolean"
      },
      "max_pages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 500
      }
    },
    "required": [
      "owner",
      "name"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
