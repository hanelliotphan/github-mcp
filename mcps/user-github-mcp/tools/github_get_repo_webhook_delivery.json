{
  "name": "github_get_repo_webhook_delivery",
  "description": "Get one webhook delivery by ID (GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}). Full delivery including request/response bodies when present. delivery_id from github_list_repo_webhook_deliveries id. Classic tokens need read:repo_hook or repo. See GitHub REST repository webhooks.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      },
      "hook_id": {
        "type": "integer",
        "minimum": 1
      },
      "delivery_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["owner", "name", "hook_id", "delivery_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
