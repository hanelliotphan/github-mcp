{
  "name": "github_get_reviews_for_workflow_run",
  "description": "Get the review history for a workflow run requiring deployment protection rule approvals (GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals). Classic tokens need repo scope for private repositories. Returns HTTP 200. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["owner", "name", "run_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
