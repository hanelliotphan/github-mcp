{
  "name": "github_review_custom_gates_for_workflow_run",
  "description": "Approve or reject a custom deployment protection rule (gate) for a workflow run's environment (POST /repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule). Provide environment_name, state (approved or rejected), comment. Returns HTTP 204. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 },
      "environment_name": { "type": "string", "minLength": 1 },
      "state": { "type": "string", "enum": ["approved", "rejected"] },
      "comment": { "type": "string", "maxLength": 65535 }
    },
    "required": ["owner", "name", "run_id", "environment_name", "state", "comment"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
