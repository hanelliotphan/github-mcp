{
  "name": "github_re_run_workflow_failed_jobs",
  "description": "Re-run all failed jobs and their dependent jobs in a workflow run (POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs). Optionally set enable_debug_logging. Classic tokens need repo scope. Returns HTTP 201. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 },
      "enable_debug_logging": { "type": "boolean" }
    },
    "required": ["owner", "name", "run_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
