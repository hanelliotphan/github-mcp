{
  "name": "github_approve_workflow_run",
  "description": "Approve a workflow run for a pull request from a public fork of a first-time contributor (POST /repos/{owner}/{repo}/actions/runs/{run_id}/approve). Classic tokens need repo scope. Returns HTTP 201. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["owner", "name", "run_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
