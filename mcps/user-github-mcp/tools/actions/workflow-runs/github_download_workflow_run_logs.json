{
  "name": "github_download_workflow_run_logs",
  "description": "Get a temporary download URL for a workflow run's log archive (GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs). GitHub responds with HTTP 302 and a Location URL for a zip archive; this returns that URL without downloading. URL expires after 1 minute. Classic tokens need repo scope for private repositories. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["owner", "name", "run_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
