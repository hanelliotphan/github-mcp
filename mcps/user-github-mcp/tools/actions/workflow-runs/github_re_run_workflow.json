{
  "name": "github_re_run_workflow",
  "description": "Re-run an entire workflow run by its id (POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun). Optionally set enable_debug_logging. Classic tokens need repo scope. Returns HTTP 201. See GitHub REST workflow runs.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "run_id": { "type": "integer", "minimum": 1 },
      "enable_debug_logging": { "type": "boolean" }
    },
    "required": ["owner", "name", "run_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
