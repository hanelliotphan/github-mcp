{
  "name": "github_get_workflow",
  "description": "Get a specific workflow (GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}). workflow_id may be the numeric id or the workflow file name (e.g. main.yaml). Anyone with read access can use this; classic tokens need repo scope for private repositories. Returns HTTP 200. See GitHub REST workflows.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "workflow_id": { "type": ["integer", "string"] }
    },
    "required": ["owner", "name", "workflow_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
