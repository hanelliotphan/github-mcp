{
  "name": "github_create_remove_token_for_org",
  "description": "Create a remove token used to remove a self-hosted runner from an organization (POST /orgs/{org}/actions/runners/remove-token). The token is passed to the runner config script with --remove and expires after one hour. Classic tokens need admin:org scope. Success is HTTP 201. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
