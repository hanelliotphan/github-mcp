{
  "name": "github_add_custom_labels_to_self_hosted_runner_for_repo",
  "description": "Add custom labels to a self-hosted runner configured in a repository (POST /repos/{owner}/{repo}/actions/runners/{runner_id}/labels). Provide labels (1-100) to append to existing labels. Returns the full updated label set. Classic tokens need repo scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^[A-Za-z0-9._-]{1,100}$" },
      "runner_id": { "type": "integer", "minimum": 1 },
      "labels": { "type": "array", "items": { "type": "string", "minLength": 1 }, "minItems": 1, "maxItems": 100 }
    },
    "required": ["owner", "name", "runner_id", "labels"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
