{
  "name": "github_list_labels_for_self_hosted_runner_for_repo",
  "description": "List all labels for a self-hosted runner configured in a repository (GET /repos/{owner}/{repo}/actions/runners/{runner_id}/labels). Returns total_count and labels (id, name, type read-only or custom). Classic tokens need repo scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^[A-Za-z0-9._-]{1,100}$" },
      "runner_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["owner", "name", "runner_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
