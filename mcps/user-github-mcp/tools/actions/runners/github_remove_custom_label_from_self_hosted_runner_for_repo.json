{
  "name": "github_remove_custom_label_from_self_hosted_runner_for_repo",
  "description": "Remove a single custom label from a self-hosted runner configured in a repository (DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name}). Returns the runner's remaining labels. Read-only labels cannot be removed. Classic tokens need repo scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^[A-Za-z0-9._-]{1,100}$" },
      "runner_id": { "type": "integer", "minimum": 1 },
      "label_name": { "type": "string", "minLength": 1 }
    },
    "required": ["owner", "name", "runner_id", "label_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
