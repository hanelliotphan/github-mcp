{
  "name": "github_list_runner_applications_for_repo",
  "description": "List binaries for the self-hosted runner application you can download and run for a repository (GET /repos/{owner}/{repo}/actions/runners/downloads). Returns an array of runner applications (os, architecture, download_url, filename). Classic tokens need repo scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^[A-Za-z0-9._-]{1,100}$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
