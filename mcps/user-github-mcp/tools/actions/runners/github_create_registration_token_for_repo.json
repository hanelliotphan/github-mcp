{
  "name": "github_create_registration_token_for_repo",
  "description": "Create a registration token for adding a self-hosted runner to a repository (POST /repos/{owner}/{repo}/actions/runners/registration-token). The token is used with the runner config script and expires after one hour. Classic tokens need repo scope. Success is HTTP 201. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^[A-Za-z0-9._-]{1,100}$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
