{
  "name": "github_remove_all_custom_labels_from_self_hosted_runner_for_org",
  "description": "Remove all custom labels from a self-hosted runner configured in an organization (DELETE /orgs/{org}/actions/runners/{runner_id}/labels). Returns the remaining read-only labels. Classic tokens need admin:org scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "runner_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "runner_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
