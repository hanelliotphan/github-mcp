{
  "name": "github_delete_self_hosted_runner_from_org",
  "description": "Forcefully remove a self-hosted runner from an organization (DELETE /orgs/{org}/actions/runners/{runner_id}). Use when the runner is offline and cannot be removed with a remove token. Classic tokens need admin:org scope. Success is HTTP 204 with no content. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "runner_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "runner_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
