{
  "name": "github_list_runner_applications_for_org",
  "description": "List binaries for the self-hosted runner application you can download and run for an organization (GET /orgs/{org}/actions/runners/downloads). Returns an array of runner applications (os, architecture, download_url, filename). Classic tokens need admin:org scope. See GitHub REST self-hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
