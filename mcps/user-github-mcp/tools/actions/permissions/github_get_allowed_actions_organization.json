{
  "name": "github_get_allowed_actions_organization",
  "description": "Get allowed actions and reusable workflows for an organization (GET /orgs/{org}/actions/permissions/selected-actions). Returns github_owned_allowed, verified_allowed, patterns_allowed. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
