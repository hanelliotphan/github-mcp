{
  "name": "github_get_github_actions_default_workflow_permissions_organization",
  "description": "Get default workflow permissions for an organization (GET /orgs/{org}/actions/permissions/workflow). Returns default_workflow_permissions and can_approve_pull_request_reviews. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
