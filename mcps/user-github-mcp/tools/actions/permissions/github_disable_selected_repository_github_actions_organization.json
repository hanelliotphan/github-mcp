{
  "name": "github_disable_selected_repository_github_actions_organization",
  "description": "Disable a selected repository for GitHub Actions in an organization (DELETE /orgs/{org}/actions/permissions/repositories/{repository_id}). Success is HTTP 204. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "repository_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "repository_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
