{
  "name": "github_list_selected_repositories_enabled_github_actions_organization",
  "description": "List selected repositories enabled for GitHub Actions in an organization (GET /orgs/{org}/actions/permissions/repositories). Returns total_count and repositories. per_page 1-100 default 100, page, all_pages, max_pages. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
