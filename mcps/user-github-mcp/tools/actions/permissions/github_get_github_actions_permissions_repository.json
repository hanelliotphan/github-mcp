{
  "name": "github_get_github_actions_permissions_repository",
  "description": "Get GitHub Actions permissions policy for a repository (GET /repos/{owner}/{repo}/actions/permissions). Returns enabled, allowed_actions, selected_actions_url. Requires repo admin; classic tokens need repo scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
