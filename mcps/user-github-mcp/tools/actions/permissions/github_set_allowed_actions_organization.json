{
  "name": "github_set_allowed_actions_organization",
  "description": "Set allowed actions and reusable workflows for an organization (PUT /orgs/{org}/actions/permissions/selected-actions). Optional github_owned_allowed, verified_allowed, patterns_allowed. Success is HTTP 204. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "github_owned_allowed": { "type": "boolean" },
      "verified_allowed": { "type": "boolean" },
      "patterns_allowed": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
