{
  "name": "github_set_github_actions_permissions_repository",
  "description": "Set GitHub Actions permissions policy for a repository (PUT /repos/{owner}/{repo}/actions/permissions). Body enabled (boolean) and optional allowed_actions (all|local_only|selected). Success is HTTP 204. Requires repo admin; classic tokens need repo scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "enabled": { "type": "boolean" },
      "allowed_actions": { "type": "string", "enum": ["all", "local_only", "selected"] }
    },
    "required": ["owner", "name", "enabled"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
