{
  "name": "github_get_allowed_actions_repository",
  "description": "Get allowed actions and reusable workflows for a repository (GET /repos/{owner}/{repo}/actions/permissions/selected-actions). Returns github_owned_allowed, verified_allowed, patterns_allowed. Requires repo admin; classic tokens need repo scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
