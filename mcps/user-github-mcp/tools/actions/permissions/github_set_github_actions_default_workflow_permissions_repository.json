{
  "name": "github_set_github_actions_default_workflow_permissions_repository",
  "description": "Set default workflow permissions for a repository (PUT /repos/{owner}/{repo}/actions/permissions/workflow). Optional default_workflow_permissions (read|write) and can_approve_pull_request_reviews. Success is HTTP 204. Requires repo admin; classic tokens need repo scope. See GitHub REST Actions permissions.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "default_workflow_permissions": { "type": "string", "enum": ["read", "write"] },
      "can_approve_pull_request_reviews": { "type": "boolean" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
