{
  "name": "github_get_oidc_custom_sub_template_for_org",
  "description": "Get the customization template for an OIDC subject claim for an organization (GET /orgs/{org}/actions/oidc/customization/sub). Returns include_claim_keys. Requires org owner; classic tokens need read:org scope. See GitHub REST Actions OIDC.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
