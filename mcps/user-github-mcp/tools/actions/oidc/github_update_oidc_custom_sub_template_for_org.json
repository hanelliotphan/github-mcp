{
  "name": "github_update_oidc_custom_sub_template_for_org",
  "description": "Set the customization template for an OIDC subject claim for an organization (PUT /orgs/{org}/actions/oidc/customization/sub). Body include_claim_keys (array of strings). Success is HTTP 201. Requires org owner; classic tokens need write:org scope. See GitHub REST Actions OIDC.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "include_claim_keys": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 }
      }
    },
    "required": ["org", "include_claim_keys"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
