{
  "name": "github_get_custom_oidc_sub_claim_for_repo",
  "description": "Get the customization template for an OIDC subject claim for a repository (GET /repos/{owner}/{repo}/actions/oidc/customization/sub). Returns use_default and include_claim_keys. Requires repo admin; classic tokens need repo scope. See GitHub REST Actions OIDC.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
