{
  "name": "github_delete_self_hosted_runner_group_from_org",
  "description": "Delete a self-hosted runner group from an organization (DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}). Classic tokens need admin:org scope. Success is HTTP 204. See GitHub REST self-hosted runner groups.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "runner_group_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "runner_group_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
