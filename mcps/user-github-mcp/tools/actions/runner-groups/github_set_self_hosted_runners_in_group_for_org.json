{
  "name": "github_set_self_hosted_runners_in_group_for_org",
  "description": "Replace the list of self-hosted runners that are part of an organization runner group (PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners). Provide runners (complete list of runner IDs). Classic tokens need admin:org scope. Success is HTTP 204. See GitHub REST self-hosted runner groups.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "runner_group_id": { "type": "integer", "minimum": 1 },
      "runners": { "type": "array", "items": { "type": "integer", "minimum": 1 } }
    },
    "required": ["org", "runner_group_id", "runners"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
