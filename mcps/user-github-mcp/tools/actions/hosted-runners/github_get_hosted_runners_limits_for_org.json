{
  "name": "github_get_hosted_runners_limits_for_org",
  "description": "Get GitHub-hosted runner limits for an organization (GET /orgs/{org}/actions/hosted-runners/limits). Returns public_ips limits (maximum, current_usage). See GitHub REST Actions hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
