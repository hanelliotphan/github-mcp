{
  "name": "github_list_custom_images_for_org",
  "description": "List custom images for GitHub-hosted runners in an organization (GET /orgs/{org}/actions/hosted-runners/images/custom). Returns total_count and images. Classic tokens need manage_runners:org scope. See GitHub REST Actions hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
