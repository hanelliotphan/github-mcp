{
  "name": "github_get_custom_image_for_org",
  "description": "Get a custom image definition for GitHub Actions Hosted Runners (GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}). Returns the image definition. Classic tokens need manage_runners:org scope. See GitHub REST Actions hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "image_definition_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "image_definition_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
