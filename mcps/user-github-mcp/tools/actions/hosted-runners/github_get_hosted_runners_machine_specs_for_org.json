{
  "name": "github_get_hosted_runners_machine_specs_for_org",
  "description": "Get machine specs available for GitHub-hosted runners in an organization (GET /orgs/{org}/actions/hosted-runners/machine-sizes). Returns total_count and machine_specs (id, cpu_cores, memory_gb, storage_gb). See GitHub REST Actions hosted runners.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
