{
  "name": "github_update_org_actions_variable",
  "description": "Update an organization GitHub Actions variable (PATCH /orgs/{org}/actions/variables/{name}). Identify by name; optionally change value, visibility (all, private, selected), and selected_repository_ids. Classic tokens need admin:org scope (plus repo for private repositories). Success is HTTP 204. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "value": { "type": "string" },
      "visibility": { "type": "string", "enum": ["all", "private", "selected"] },
      "selected_repository_ids": { "type": "array", "items": { "type": "integer", "minimum": 1 } }
    },
    "required": ["org", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
