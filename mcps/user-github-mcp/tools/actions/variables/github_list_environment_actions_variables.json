{
  "name": "github_list_environment_actions_variables",
  "description": "List GitHub Actions variables in a repository environment (GET /repos/{owner}/{repo}/environments/{environment_name}/variables). Returns total_count and variables (name, value, timestamps). per_page 1-30 default 30, page, all_pages, max_pages. Classic tokens need repo scope. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "environment_name": { "type": "string", "minLength": 1 },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 30 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["owner", "name", "environment_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
