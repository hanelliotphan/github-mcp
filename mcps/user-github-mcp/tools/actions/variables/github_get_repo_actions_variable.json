{
  "name": "github_get_repo_actions_variable",
  "description": "Get a single repository GitHub Actions variable (GET /repos/{owner}/{repo}/actions/variables/{name}). Returns name, value, and timestamps. Classic tokens need repo scope. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "variable_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" }
    },
    "required": ["owner", "name", "variable_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
