{
  "name": "github_update_environment_actions_variable",
  "description": "Update a GitHub Actions variable's value in a repository environment (PATCH /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}). Identify by variable_name and provide a new value. Classic tokens need repo scope. Success is HTTP 204. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "environment_name": { "type": "string", "minLength": 1 },
      "variable_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "value": { "type": "string" }
    },
    "required": ["owner", "name", "environment_name", "variable_name", "value"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
