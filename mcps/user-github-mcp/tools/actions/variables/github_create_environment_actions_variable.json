{
  "name": "github_create_environment_actions_variable",
  "description": "Create a GitHub Actions variable in a repository environment (POST /repos/{owner}/{repo}/environments/{environment_name}/variables). Provide variable_name and value. Classic tokens need repo scope. Success is HTTP 201. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "environment_name": { "type": "string", "minLength": 1 },
      "variable_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "value": { "type": "string" }
    },
    "required": ["owner", "name", "environment_name", "variable_name", "value"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
