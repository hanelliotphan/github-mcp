{
  "name": "github_remove_selected_repo_from_org_actions_variable",
  "description": "Revoke one repository's access to a selected-visibility organization variable (DELETE /orgs/{org}/actions/variables/{name}/repositories/{repository_id}). HTTP 409 if visibility is not selected. Classic tokens need admin:org scope (plus repo for private repositories). Success is HTTP 204. See GitHub REST Actions variables.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "repository_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["org", "name", "repository_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
