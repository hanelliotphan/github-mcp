{
  "name": "github_get_artifact",
  "description": "Get a specific GitHub Actions artifact (GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}). Returns the artifact object (id, name, size_in_bytes, archive_download_url, expired, created_at, expires_at, workflow_run, ...). artifact_id from github_list_artifacts_for_repo or github_list_workflow_run_artifacts. Classic tokens need repo scope for private repos. HTTP 200. See GitHub REST Actions artifacts.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      },
      "artifact_id": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["owner", "name", "artifact_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
