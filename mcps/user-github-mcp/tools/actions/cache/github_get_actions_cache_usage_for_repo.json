{
  "name": "github_get_actions_cache_usage_for_repo",
  "description": "Get GitHub Actions cache usage for a repository (GET /repos/{owner}/{repo}/actions/cache/usage). Returns full_name, active_caches_size_in_bytes, active_caches_count. Classic tokens need repo scope for private repos. See GitHub REST Actions cache.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      },
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$"
      }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
