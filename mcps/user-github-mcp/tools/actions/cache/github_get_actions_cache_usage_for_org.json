{
  "name": "github_get_actions_cache_usage_for_org",
  "description": "Get total GitHub Actions cache usage for an organization (GET /orgs/{org}/actions/cache/usage). Returns total_active_caches_size_in_bytes, total_active_caches_count. Requires org owner; classic tokens need admin:org scope. See GitHub REST Actions cache.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "minLength": 1,
        "maxLength": 39,
        "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$"
      }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
