{
  "name": "github_set_selected_repos_for_org_actions_secret",
  "description": "Replace the full set of repositories that can access a selected-visibility organization GitHub Actions secret (PUT /orgs/{org}/actions/secrets/{secret_name}/repositories). Provide selected_repository_ids (complete list). Classic tokens need admin:org scope (plus repo for private repos). Success is HTTP 204. See GitHub REST Actions secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "secret_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "selected_repository_ids": { "type": "array", "items": { "type": "integer", "minimum": 1 } }
    },
    "required": ["org", "secret_name", "selected_repository_ids"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
