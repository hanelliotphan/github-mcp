{
  "name": "github_delete_environment_actions_secret",
  "description": "Delete an environment GitHub Actions secret by name (DELETE /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}). Requires collaborator access; classic tokens need repo scope. Success is HTTP 204. See GitHub REST Actions secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "environment_name": { "type": "string", "minLength": 1 },
      "secret_name": { "type": "string", "minLength": 1, "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" }
    },
    "required": ["owner", "name", "environment_name", "secret_name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
