{
  "name": "github_list_org_actions_secrets",
  "description": "List GitHub Actions secrets in an organization without revealing their values (GET /orgs/{org}/actions/secrets). Returns total_count and secrets (name, timestamps, visibility). per_page 1-100 default 100, page, all_pages, max_pages. Classic tokens need admin:org scope (plus repo for private repos). See GitHub REST Actions secrets.",
  "arguments": {
    "type": "object",
    "properties": {
      "org": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["org"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
