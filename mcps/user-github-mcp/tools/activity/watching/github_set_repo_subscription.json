{
  "name": "github_set_repo_subscription",
  "description": "Set the authenticated user's subscription to a repository (PUT /repos/{owner}/{repo}/subscription). subscribed true to watch, ignored true to block all notifications. To stop watching entirely, delete the subscription instead. Returns HTTP 200. See GitHub REST activity watching.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "subscribed": { "type": "boolean" },
      "ignored": { "type": "boolean" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
