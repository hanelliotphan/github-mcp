{
  "name": "github_list_repos_watched_by_user",
  "description": "List repositories a user is watching (GET /users/{username}/subscriptions). Deprecated as of July 2026 (returns empty responses during deprecation). per_page 1-100 default 30, page, all_pages, max_pages. See GitHub REST activity watching.",
  "arguments": {
    "type": "object",
    "properties": {
      "username": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["username"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
