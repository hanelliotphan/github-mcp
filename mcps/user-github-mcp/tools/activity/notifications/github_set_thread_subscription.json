{
  "name": "github_set_thread_subscription",
  "description": "Set the authenticated user's subscription to a notification thread (PUT /notifications/threads/{thread_id}/subscription). Set ignored true to block all future notifications, or false to (re)subscribe. Requires classic PAT with notifications or repo scope. Returns HTTP 200. See GitHub REST activity notifications.",
  "arguments": {
    "type": "object",
    "properties": {
      "thread_id": { "type": "integer", "minimum": 1 },
      "ignored": { "type": "boolean" }
    },
    "required": ["thread_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
