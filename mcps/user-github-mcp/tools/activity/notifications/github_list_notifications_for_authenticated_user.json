{
  "name": "github_list_notifications_for_authenticated_user",
  "description": "List all notifications (threads) for the authenticated user, sorted by most recently updated (GET /notifications). Requires classic PAT with notifications or repo scope. Filters all, participating, since, before (ISO 8601). per_page 1-50 default 50, page, all_pages, max_pages. See GitHub REST activity notifications.",
  "arguments": {
    "type": "object",
    "properties": {
      "all": { "type": "boolean" },
      "participating": { "type": "boolean" },
      "since": { "type": "string" },
      "before": { "type": "string" },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 50 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": [],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
