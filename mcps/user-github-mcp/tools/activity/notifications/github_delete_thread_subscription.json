{
  "name": "github_delete_thread_subscription",
  "description": "Delete the authenticated user's subscription to a notification thread (DELETE /notifications/threads/{thread_id}/subscription). Mutes future notifications until you comment or are @mentioned. Requires classic PAT with notifications or repo scope. Returns HTTP 204. See GitHub REST activity notifications.",
  "arguments": {
    "type": "object",
    "properties": {
      "thread_id": { "type": "integer", "minimum": 1 }
    },
    "required": ["thread_id"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
