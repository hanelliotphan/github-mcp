{
  "name": "github_list_repo_notifications_for_authenticated_user",
  "description": "List all notifications for the authenticated user in a repository (GET /repos/{owner}/{repo}/notifications). Requires classic PAT with notifications or repo scope. Filters all, participating, since, before (ISO 8601). per_page 1-100 default 30, page, all_pages, max_pages. See GitHub REST activity notifications.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "all": { "type": "boolean" },
      "participating": { "type": "boolean" },
      "since": { "type": "string" },
      "before": { "type": "string" },
      "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
      "page": { "type": "integer", "minimum": 1 },
      "all_pages": { "type": "boolean" },
      "max_pages": { "type": "integer", "minimum": 1, "maximum": 500 }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
