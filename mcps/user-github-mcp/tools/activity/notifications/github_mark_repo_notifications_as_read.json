{
  "name": "github_mark_repo_notifications_as_read",
  "description": "Mark all notifications in a repository as read for the authenticated user (PUT /repos/{owner}/{repo}/notifications). Requires classic PAT with notifications or repo scope. Optional last_read_at (ISO 8601). Returns HTTP 202 (async) or 205. See GitHub REST activity notifications.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" },
      "last_read_at": { "type": "string" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
