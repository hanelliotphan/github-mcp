{
  "name": "github_check_repo_is_starred_by_authenticated_user",
  "description": "Check whether the authenticated user has starred a repository (GET /user/starred/{owner}/{repo}). Returns 204 when starred and 404 when not; mapped to a boolean starred field. See GitHub REST activity starring.",
  "arguments": {
    "type": "object",
    "properties": {
      "owner": { "type": "string", "minLength": 1, "maxLength": 39, "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9]|-(?=[A-Za-z0-9-]*[A-Za-z0-9])){0,38}$" },
      "name": { "type": "string", "minLength": 1, "maxLength": 100, "pattern": "^(?![.-])[A-Za-z0-9._-]{1,100}(?<![.-])$" }
    },
    "required": ["owner", "name"],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
